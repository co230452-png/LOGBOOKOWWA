# Backend API URL
# For local development, the Vite dev server proxies /api to localhost:5000 (see vite.config.ts)
# Set this explicitly if your backend runs on a different host/port, or for production builds
VITE_API_URL=http://localhost:5000/api
